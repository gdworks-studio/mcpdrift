.venv/
__pycache__/
*.py[cod]
.pytest_cache/
dist/
build/
*.egg-info/

# mcpdrift's own scratch (not part of the published package)
.codex-reports/
# Ignore mcpdrift scratch, but track the dogfood baseline the CI diffs against.
/.mcpdrift/*
!/.mcpdrift/contract.json

# macOS
.DS_Store
._*
